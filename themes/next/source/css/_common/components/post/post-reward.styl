.reward-container {
  margin: 20px auto;
  padding: 10px 0;
  text-align: center;
  width: 90%;

  button {
    background: $red;
    border: 0;
    border-radius: $border-radius-base;
    color: white;
    cursor: pointer;
    line-height: 2;
    outline: 0;
    padding: 0 30px;
    vertical-align: text-top;

    &:hover {
      background: lighten(#d48806, 20%);
    }
  }
}

#qr {
  padding-top: 10px;

  a {
    border: 0;
  }

  img {
    display: inline-block;
    margin: .8em 2em 0 2em;
    max-width: 100%;
    width: 220px;
  }

  p {
    text-align: center;
  }

  if (hexo-config('reward_settings.animation')) {
    > div:hover p {
      animation: roll .1s infinite linear;
    }

    @keyframes roll {
      from {
        transform: rotateZ(30deg);
      }

      to {
        transform: rotateZ(-30deg);
      }
    }
  }
}
